# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Node (if any tooling is added later)
node_modules/

# Claude Code local-only settings (keep launch.json; ignore machine-specific overrides)
.claude/settings.local.json
